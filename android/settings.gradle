pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "com.android.application" version "7.4.2"
        id "org.jetbrains.kotlin.android" version "1.9.0"
        id "dev.flutter.flutter-gradle-plugin" version "1.0.0"
    }
}

rootProject.name = "magic_note"
include ":app"
